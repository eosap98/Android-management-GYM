package com.gymku.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background & Surface ──────────────────────────────────────────────────────
val Slate50   = Color(0xFFF8FAFC)
val White     = Color(0xFFFFFFFF)
val Slate100  = Color(0xFFF1F5F9)
val Slate200  = Color(0xFFE2E8F0)  // Border/Divider

// ── Text ──────────────────────────────────────────────────────────────────────
val Slate900  = Color(0xFF0F172A)  // text primary
val Slate500  = Color(0xFF64748B)  // text secondary
val Slate400  = Color(0xFF94A3B8)  // text placeholder

// ── Action Colors ─────────────────────────────────────────────────────────────
val IndigoMain  = Color(0xFF4F46E5)  // Primary CTA
val IndigoDark  = Color(0xFF3730A3)
val IndigoLight = Color(0xFFEEF2FF)

// ── Status Colors ─────────────────────────────────────────────────────────────
val EmeraldGreen  = Color(0xFF10B981)  // Active member
val EmeraldLight  = Color(0xFFD1FAE5)
val RoseRed       = Color(0xFFF43F5E)  // Expired member
val RoseLight     = Color(0xFFFFE4E6)
val AmberYellow   = Color(0xFFF59E0B)  // Warning / shift
val AmberLight    = Color(0xFFFEF3C7)

// ── Dark Overlays ─────────────────────────────────────────────────────────────
val Slate800  = Color(0xFF1E293B)  // Header / FAB
val Slate700  = Color(0xFF334155)
val Slate600  = Color(0xFF475569)
